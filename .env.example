# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/smart_timing

# Server Configuration
PORT=4000

# Frontend Origins (comma-separated)
FRONTEND_ORIGINS=https://smart-timing-git-main-daniel-qazis-projects.vercel.app,http://localhost:3000,http://127.0.0.1:3000

# Frontend Origin Suffixes (comma-separated, for dynamic Vercel deployments)
FRONTEND_ORIGIN_SUFFIXES=.vercel.app

# Google Sheets Integration (for Kinoa Tiltak AS projects)
# OAuth2 Method (Recommended) - Users authenticate with their own Google account
# Get these from Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:4000/api/auth/google/callback
FRONTEND_URL=http://localhost:3000

# Service Account Impersonation (Optional)
# Enable this if you want to impersonate a service account using user OAuth tokens
# User must have roles/iam.serviceAccountTokenCreator on the service account
GOOGLE_USE_IMPERSONATION=false
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service Account Method (Legacy/Optional) - For server-to-server authentication
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour\nPrivate\nKey\nHere\n-----END PRIVATE KEY-----\n"
